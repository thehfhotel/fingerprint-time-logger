# ============================================================================
# FINGERPRINT TIME LOGGER - ENVIRONMENT CONFIGURATION
# ============================================================================
# Copy this file to .env and customize values for your environment
# All settings have sensible defaults and can be overridden via environment variables

# ========================================================================
# DATABASE CONFIGURATION
# ========================================================================
FINGERPRINT_DATABASE_URL=sqlite:///./attendance.db

# ========================================================================
# ZKTECO DEVICE CONFIGURATION
# ========================================================================
FINGERPRINT_ZKTECO_HOST=192.168.100.209
FINGERPRINT_ZKTECO_PORT=4370
FINGERPRINT_ZKTECO_PASSWORD=0
FINGERPRINT_ZKTECO_TIMEOUT=5
FINGERPRINT_ZKTECO_MAX_RETRIES=3

# ========================================================================
# SERVER CONFIGURATION
# ========================================================================
FINGERPRINT_SERVER_HOST=0.0.0.0
FINGERPRINT_SERVER_PORT=5000
FINGERPRINT_SECRET_KEY=fingerprint-time-logger-2025

# ========================================================================
# LOGGING
# ========================================================================
FINGERPRINT_LOG_LEVEL=INFO

# ========================================================================
# PERFORMANCE & SYNC
# ========================================================================
FINGERPRINT_SYNC_INTERVAL_SECONDS=30
FINGERPRINT_CLEAR_DEVICE_AFTER_SYNC=false

# ========================================================================
# EXPORT SETTINGS
# ========================================================================
FINGERPRINT_EXPORT_MAX_RECORDS=50000
FINGERPRINT_EXPORT_TIMEOUT_SECONDS=300
FINGERPRINT_EXPORT_DEFAULT_FORMAT=csv

# ========================================================================
# ENVIRONMENT
# ========================================================================
FINGERPRINT_ENVIRONMENT=development  # development or production
FINGERPRINT_DEBUG=true

# ========================================================================
# LINE OAUTH CONFIGURATION (Phase 2 - QR Check-in Feature)
# ========================================================================
# Get these values from LINE Developers Console: https://developers.line.biz/console/
LINE_CHANNEL_ID=your_channel_id
LINE_CHANNEL_SECRET=your_channel_secret
LINE_CALLBACK_URL=https://emp.thehfhotel.org/fingerprintlogs/api/auth/line/callback

# JWT Secret for mobile session tokens (change in production)
JWT_SECRET=your-secret-key-change-in-production

# Auto-import interval for fingerprint logs (minutes)
AUTO_IMPORT_INTERVAL_MINUTES=30