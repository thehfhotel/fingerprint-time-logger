{
  "name": "qr-checkin-proxy-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for QR check-in URL rewriting",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dev": "wrangler deploy --env development",
    "deploy:prod": "wrangler deploy --env production",
    "tail": "wrangler tail",
    "test": "vitest"
  },
  "keywords": ["cloudflare", "workers", "qr-checkin", "proxy"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.22.0",
    "vitest": "^1.0.0"
  }
}
