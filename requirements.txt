# Core API & Web Server
fastapi==0.104.1
uvicorn[standard]==0.24.0
flask==3.0.0
flask-socketio==5.3.6

# Database & ORM (SQLite-focused for single-user)
sqlalchemy==2.0.23
alembic==1.12.1

# ZKTeco Device Integration
pyzk==0.9

# Data Validation & HTTP
pydantic==2.5.0
pydantic-settings==2.1.0
python-multipart==0.0.6
httpx==0.25.2

# Environment & Configuration
python-dotenv==1.0.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1

# Optional: Remove if not using PostgreSQL/Redis/Celery
# psycopg2-binary==2.9.9  # Only needed for PostgreSQL
# redis==5.0.1            # Only needed for caching
# celery==5.3.4           # Only needed for background tasks
# websockets==12.0        # Only needed for advanced WebSocket features